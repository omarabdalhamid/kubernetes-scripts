apiVersion: v1
kind: Service
metadata:
  name: nginx-odoo-svc
  labels:
   app-svc: nginx-odoo-svc2019
spec:
   type: NodePort
   ports:
   - port: 8089
     name: http-8089
     targetPort: 80
   selector:
     app: nginx-ssl

---

apiVersion: apps/v1
kind: Deployment
metadata:
   name: nginx2019
spec:
   selector:
     matchLabels:
       app: nginx-ssl
   replicas: 4
   template:
     metadata:
       labels:
         app: nginx-ssl
     spec:
       containers:
        - name: nginx-odoo2019
          image: nginx
          ports:
          - containerPort: 80
